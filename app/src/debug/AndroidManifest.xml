<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Debug-only: allow cleartext HTTP to a locally running backend (e.g. http://10.0.2.2:8080).
         Release builds keep blocking http:// so production must use https. -->
    <application android:usesCleartextTraffic="true" />

</manifest>
